# Task 4: Settings and integrations

- Add a real settings route for goal, intensity, and language.
- Connect the current agent fields—name, role, template, objective, instructions, tone, tools, and context—to validated backend contracts.
- Use one shared template/capability catalog for onboarding, creation, and editing.
- Connect the existing agent roster, run timeline, cancellation, duplication, pause, and archive controls without adding new dashboard UI.
- Add GitHub and Telegram connection states with backend-ready service actions.
- Provide explicit unavailable/error states where the real backend is required.
- Keep the current settings structure and styling while correcting service/state behavior required for reliable mutations.
- Keep `agentId` stable across frontend state, backend contracts, and persisted data.
