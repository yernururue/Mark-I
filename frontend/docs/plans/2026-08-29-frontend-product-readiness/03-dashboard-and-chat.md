# Task 3: Dashboard and chat

- Make the dashboard the central workspace for the agent roster, concurrent runs, outputs, mentor-specific progress, observations, and decisions.
- Add coherent loading, empty, error, and retry states.
- Implement agent and group conversation selection without scattered mock data.
- Implement multiline chat composition, Enter/Shift+Enter behavior, send, optimistic user messages, pending assistant state, failures, retry, and auto-scroll.
- Keep chat, agent, run, artifact, and handoff operations behind typed service contracts with a replaceable local adapter.
- Make primary navigation and chat usable on mobile.
